{
  "dependencies": ["."],
  "graphs": {
    "weather_agent": {
      "path": "./backend/src/agents/weather_agent.py:create_weather_agent_graph",
      "description": "Production weather agent with RAG + CoT (configure via .env: ENABLE_RAG/ENABLE_COT, override at runtime via configurable)"
    },
    "weather_hitl_workflow": {
      "path": "./backend/src/workflows/weather_graph.py:get_weather_hitl_workflow",
      "description": "Level 1: HITL workflow with hurricane approval for Category 3+ storms"
    }
  },
  "env": ".env",
  "http": {
    "port": 2024,
    "host": "127.0.0.1"
  }
}
